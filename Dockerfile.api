# ============================================================
# ⚙️ FastAPI Backend Dockerfile (최적화 버전)
# ------------------------------------------------------------
# Base: Python 3.10 slim
# 목적: API 서버용 경량 컨테이너 이미지 빌드
# ============================================================

FROM python:3.10-slim AS runtime

# ------------------------------------------------------------
# 1. 시스템 패키지 설치
# ------------------------------------------------------------
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    libssl-dev \
    libffi-dev \
    curl \
    && rm -rf /var/lib/apt/lists/*

# ------------------------------------------------------------
# 2. 작업 디렉토리 설정
# ------------------------------------------------------------
WORKDIR /app

# ------------------------------------------------------------
# 3. 의존성 설치
# ------------------------------------------------------------
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# ------------------------------------------------------------
# 4. 애플리케이션 코드 복사
# ------------------------------------------------------------
COPY ./src ./src
COPY ./dataset ./dataset
COPY ./models ./models

# ------------------------------------------------------------
# 5. 환경 설정 파일 복사 (.env.safe → .env)
#    ⚠️ CI/CD 환경에서는 .env 파일이 GitHub에 포함되지 않으므로,
#       테스트용 .env.safe를 대신 사용합니다.
# ------------------------------------------------------------
COPY .env.safe ./.env

# ------------------------------------------------------------
# 6. FastAPI 서버 환경 변수 설정
# ------------------------------------------------------------
ENV PYTHONUNBUFFERED=1
ENV PYTHONDONTWRITEBYTECODE=1
ENV UVICORN_PORT=8000

EXPOSE 8000

# ------------------------------------------------------------
# 7. 실행 명령
# ------------------------------------------------------------
# uvicorn으로 FastAPI 서버 실행 (핫리로드 없이 프로덕션 모드)
CMD ["uvicorn", "src.api.api:app", "--host", "0.0.0.0", "--port", "8000"]
