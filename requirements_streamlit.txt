# ----------------------------------------------------------------------
# MLOps Cloud Project Python Dependencies
# (Streamlit UI 프론트엔드 컨테이너 전용)
# ----------------------------------------------------------------------
# 💡 컨테이너 최적화를 위해 UI 실행에 필요한 최소한의 패키지만 포함합니다.
#    (FastAPI 서버용 패키지(예: lightgbm, joblib)는 제외됩니다.)

# 1. UI 프레임워크 (필수)
streamlit

# 2. 데이터 처리 및 API 통신
pandas       # 데이터프레임 처리 및 UI 표시
requests     # FastAPI 백엔드 API 호출

# 3. 데이터베이스 연결 (모니터링 탭 등에 필요)
pymysql

# ----------------------------------------------------------------------
# [목록이 간결한 이유]
# - 경량화: 이 파일만 사용하여 Docker 이미지를 빌드하면 불필요한 라이브러리가 
#   설치되지 않아 컨테이너 크기가 최소화되고 시작 속도가 빨라집니다.
# - 확장성: 백엔드 모델(API)과 프론트엔드(UI)의 종속성이 완전히 분리되어, 
#   각각 독립적으로 업데이트 및 확장이 가능합니다.
# ----------------------------------------------------------------------