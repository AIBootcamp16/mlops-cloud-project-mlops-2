# ============================================================
# 🚫 Dockerignore for MLOps Music Recommender
# ------------------------------------------------------------
# 목적:
#   - 빌드 시 대용량 또는 민감 파일을 Docker 컨텍스트에서 제외
#   - 컨테이너 이미지 크기 최소화 및 빌드 캐시 향상
#   - 단, 서비스 실행에 필요한 설정(.env 등)은 포함 허용
# ============================================================


# ---------------------------------
# 🧹 Python 캐시 / 임시 파일
# ---------------------------------
__pycache__/
*.py[cod]
*$py.class
*.log
*.tmp
*.bak


# ---------------------------------
# ⚙️ 가상환경 / 개발 설정
# ---------------------------------
.venv/
venv/
Pipfile
Pipfile.lock
*.iml
.idea/
.vscode/
logs/
tmp/
__MACOSX/


# ---------------------------------
# 🌍 환경 설정 파일 (.env)
# ---------------------------------
# ⚠️ 기본적으로 .env 파일은 보안 이유로 제외하는 것이 일반적입니다.
#    하지만 이 프로젝트에서는 컨테이너 빌드 중 FastAPI/UI가 환경변수를 필요로 합니다.
#    따라서 .env를 빌드 컨텍스트에 포함해야 COPY 가능하므로, 아래 두 줄은 주석 처리했습니다.
#
#    (주의) .env에 실제 비밀정보가 포함되어 있다면,
#    CI/CD 시에는 .env.safe 또는 build-arg로 대체하는 것이 안전합니다.
#
#.env
#*.env


# ---------------------------------
# 📊 데이터셋 및 모델 아티팩트
# ---------------------------------
dataset/*.csv
dataset/*/*.csv
dataset/raw/
dataset/processed/
models/*.pkl
models/*.bin
models/*.joblib
mlruns/
artifacts/
checkpoints/
dags/__pycache__/
dags/*.pyc


# ---------------------------------
# 🐳 Git / Docker / 문서 관련
# ---------------------------------
.git
.gitignore
.dockerignore
README.md
LICENSE
*.md


# ---------------------------------
# 🧪 테스트 / 노트북 제외
# ---------------------------------
tests/
notebooks/
*.ipynb
