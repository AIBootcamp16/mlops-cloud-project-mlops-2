version: "3.9"

services:
  mlops-dev:
    build: .
    container_name: train_test
    env_file:
      - .env
    volumes:
      - ./mlops-cloud-project-mlops-2:/app/mlops  # 로컬 코드 공유
    working_dir: /app/mlops/src
    tty: true
    command: bash
    
